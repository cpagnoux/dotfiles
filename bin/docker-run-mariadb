#!/bin/bash

if [[ "$#" != 1 ]]; then
	echo "Usage: $0 MYSQL_ROOT_PASSWORD"
	exit
fi

root_password="$1"

docker run -d \
	--name mariadb \
	-e MYSQL_ROOT_PASSWORD="$root_password" \
	-p 3306:3306 \
	-v mariadb_mysql:/var/lib/mysql \
	mariadb
